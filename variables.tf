variable "jenkins_x_pipeline_bot_username" {
  description = "Username used by pipelines to interact with git repositories"
  type        = string
}

variable "jenkins_x_pipeline_bot_token" {
  description = "Access Token used by pipelines to interact with git repositories"
  type        = string
}
